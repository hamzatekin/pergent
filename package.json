{
  "name": "pergent",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "task": "node --disable-warning=ExperimentalWarning src/cli.ts",
    "serve": "node --disable-warning=ExperimentalWarning src/server.ts",
    "dev": "node --disable-warning=ExperimentalWarning --watch src/server.ts & tailwindcss -i web/styles.css -o dist/styles.css --watch; kill %1",
    "build": "rm -rf dist && mkdir -p dist && cp -R web/public/. dist/ && cp web/app.js dist/ && tailwindcss -i web/styles.css -o dist/styles.css --minify",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "@types/node": "^24.0.0",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "hono": "^4.13.8",
    "marked": "^18.0.13"
  }
}
